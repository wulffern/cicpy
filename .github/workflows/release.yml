name: Release

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check release tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          version="$(python - <<'PY'
          import tomllib

          with open("pyproject.toml", "rb") as f:
              print(tomllib.load(f)["project"]["version"])
          PY
          )"
          tag="${GITHUB_REF_NAME}"
          if [ "$tag" != "$version" ]; then
            echo "Tag '$tag' does not match package version '$version'."
            exit 1
          fi

      - name: Build distribution artifacts
        run: python -m pip install --upgrade build

      - name: Build package
        run: python -m build

      - name: Check distribution artifacts
        run: python -m pip install --upgrade twine && python -m twine check dist/*

      - name: Upload distribution artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  publish:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write

    environment:
      name: pypi

    steps:
      - name: Download distribution artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # A tag run whose files are already on PyPI (a manual
          # `make upload` first, or a re-run) must not die on
          # "400 File already exists" -- 0.3.1 did, and the
          # github-release job downstream never ran. PyPI rejects
          # CHANGED content for an existing filename either way, so
          # this only skips true duplicates.
          skip-existing: true

  github-release:
    runs-on: ubuntu-latest
    needs:
      - build
      - publish
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download distribution artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
