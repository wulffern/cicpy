source "https://rubygems.org"

# Local preview parity with the GitHub Pages build:
#   bundle install && bundle exec jekyll serve
# CI builds with actions/jekyll-build-pages and the remote theme; the
# gem here is the same theme for local work.
gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.8"
gem "jekyll-seo-tag"
gem "webrick"
