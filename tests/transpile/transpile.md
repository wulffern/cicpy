# transpile

`transpile` fans a `.cic` design out into multiple downstream formats.

<!--run_output:
run: make test && find SAR9B -maxdepth 1 -type f | wc -l && find SAR9B -maxdepth 1 -type f | sed 's#^#/#' | sort | head -n 12
-->

Magic output excerpt:

<!--cat:
file: SAR9B/ALGIC001_SAR9B_CV.mag
language: text
lines: 20
-->

Xschem output excerpt:

<!--cat:
file: SAR9B/ALGIC001_SAR9B_CV.sch
language: text
lines: 20
-->
